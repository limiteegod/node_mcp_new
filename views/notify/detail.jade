doctype html
html(lang="en")
    head
        include ../includes/script.jade
        script(type='text/javascript').
            KISSY.use("io,node,json,vs-data-select,vs-window", function(S, Io, Node, Json, VsDataSelect, VsWindow){

                var getData = function(){
                    var data = {};
                    return data;
                };

                var getCond = function()
                {
                    var tId = Node.one("#tId").val();
                    return {_id:tId};
                };

                var checkParam = function(data){
                    return true;
                };

                Node.one("#urlBt").on("click", function(){

                });
            });
    body
        input(id="tId", type="hidden", value=rst._id)
        #tWin.container
        .bodyAdminDiv
            .clearfix(style="margin-top:8px")
                .vs_grid_plain(style="width:100px;text-align:right;margin-top:4px") id:
                .vs_grid_plain(style="margin-left:8px;")=rst._id
            .clearfix(style="margin-top:8px")
                .vs_grid_plain(style="width:100px;text-align:right;margin-top:4px") 消息体:
                .vs_grid_plain(style="width:480px;margin-left:8px;")
                    textarea(cols=64, rows=16)=rst.msg