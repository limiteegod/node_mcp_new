var OrderType = function(){
    var self = this;
    self.info = [
        {id:0, code:'customer', des:'普通用户'},
        {id:1, code:'channel', des:'渠道'}
    ];
    self.infoArray = {};
    self.init();
};

OrderType.prototype.init = function()
{
    var self = this;
    for(var key in self.info)
    {
        var set = self.info[key];
        self.infoArray[set.id] = set;
        self[set.code] = set.id;
    };
};

OrderType.prototype.getInfoById = function(id)
{
    var self = this;
    var obj = self.info;
    if(id != undefined)
    {
        obj = self.infoArray;
        obj = obj[id];
    }
    return obj;
};

module.exports = new OrderType();